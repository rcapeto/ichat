const doc = '/api-docs'

const auth = {
  login: '/auth/login',
  session: '/auth/session',
}

const appRoutes = {
  logs: '/logs',
}

export const endpoints = {
  doc,
  authentication: auth,
  app: appRoutes,
}
