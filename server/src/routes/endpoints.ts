const doc = '/api-docs'

const auth = {
  login: '/auth/login',
  register: '/auth/register',
}

const appRoutes = {
  logs: '/logs',
}

export const endpoints = {
  doc,
  authentication: auth,
  app: appRoutes,
}
